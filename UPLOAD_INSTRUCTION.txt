REAL FACTORY V799.4 — WORKING VIEW PRESERVED

This revision starts from the working V799.2 Alter JS.

KEPT UNCHANGED:
- Current Job cards
- Lot cards
- OPEN RANDOM QUEUE cards
- Existing Assign / Submit visual workflow
- Selective Bulk Assign / Bulk Submit
- Alter / Remake / Damage flows
- V789 department view

ONLY TWO REPAIRS:
1) Cutting Colour×Size Qty resolution:
   actual_qty is now the first authoritative Qty source.
2) Completed-department temporary hide:
   When you select a Department in BULK ASSIGN, ONLY the checkbox table row
   for an OPEN Colour that already completed that Department is temporarily hidden.
   It does NOT hide/change Current Job cards or OPEN RANDOM QUEUE cards.

Example:
- C1 completed KR, C2 not completed KR -> select KR -> C1 table row hides, C2 remains.
- C1 and C2 both completed PRINT -> select PRINT -> both table rows hide.
- Change Department -> rows are recalculated; history is never deleted.

UPLOAD ONLY:
real-universal-production-v765-independent-alter.js

HTML cache line:
<script src="real-universal-production-v765-independent-alter.js?v=7994-working-view-qty-table-hide"></script>

Do NOT replace V789.
No SQL required for this JS revision.
