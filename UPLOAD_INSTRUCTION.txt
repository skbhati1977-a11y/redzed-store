REAL FACTORY V799.3 — ROOT CUTTING QTY MAP + COMPLETED DEPARTMENT TEMP HIDE

ROOT FIX:
1) Cutting Colour×Size Qty now reads actual_qty first (authoritative release qty).
   Legacy cutting_qty/main_qty/inbound_qty/assigned_qty/qty remain fallback-only.
2) Bulk/Selected Assign stays single/random-multiple/all.
3) When an Assign Department is selected, any OPEN Colour that already completed
   that Department is temporarily hidden from the Assign table and excluded from
   SELECT ALL ELIGIBLE. Nothing is deleted or rewritten.
4) Existing Submit / Alter / Remake / Damage balance logic is unchanged.

UPLOAD:
Replace ONLY:
  real-universal-production-v765-independent-alter.js

Keep your current HTML V765 cache line but bump it to:
  <script src="real-universal-production-v765-independent-alter.js?v=7993-actual-qty-completion-hide"></script>

Do NOT replace V789.
No SQL required for this JS repair.

Optional:
The ZIP also contains the already badge-hidden real-data-mode-controller-v786.js
from V799.2. Replace it only if the TEST badge is still visible.
