name: TEST71 Current Checkpoint E2E

on:
  push:
    branches:
      - test71-real-chat-e2e-finalization
    paths:
      - api/e2e-session.js
      - api/_github-oidc.js
      - tests/e2e/**
      - supabase/migrations/**
      - scripts/e2e-env-status.js
      - playwright.config.js
      - package.json
      - package-lock.json
      - .github/workflows/test71-checkpoint0-e2e.yml
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: test71-e2e-${{ github.ref }}
  cancel-in-progress: false

jobs:
  checkpoint0:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      E2E_BASE_URL: https://redzed-test65-git-test71-real-chat-e2e-6adad3-skbhati1977-4414.vercel.app
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run e2e:env-status
      - run: npm run test:e2e:static
      - run: npm run test:e2e:mc1
      - run: npx playwright test --project=cb-department
      - run: npx playwright test --project=material-unit
      - run: npm run test:e2e:checkpoint8
      - name: Upload Playwright evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: checkpoint8-playwright-evidence
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore
          retention-days: 7
