(()=>{
'use strict';
const $=id=>document.getElementById(id);
const safe=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const KEYS=[['menu_enabled','Market Menu'],['seller_workspace_enabled','Seller Workspace'],['customer_groups_enabled','Customer Groups'],['send_collection_enabled','Send Collection'],['receive_requirement_enabled','Receive Requirement'],['forward_requirement_enabled','Forward to REDZED'],['pi_convert_enabled','PI Convert'],['ci_convert_enabled','CI Convert']];
let rows=[];
function client(){return window.supabaseClient||window.sb||window.redzedSupabase||window.supabaseDb||null}
function accessOf(r){const a=r.access||{};return Object.fromEntries(KEYS.map(([k])=>[k,!!a[k]]))}
function render(){const root=$('tab-market-access');if(!root)return;const q=String($('marketAccessSearch')?.value||'').trim().toLowerCase();const filtered=rows.filter(r=>!q||String(r.name||'').toLowerCase().includes(q)||String(r.mobile||'').includes(q)||String(r.role_code||'').toLowerCase().includes(q));root.innerHTML=`<div class="toolbar"><label class="grow"><span>Search Staff / Customer</span><input id="marketAccessSearch" value="${safe(q)}" placeholder="Name / mobile / role"></label><button id="marketAccessRefresh" class="secondary">Refresh</button></div><div class="panel"><div class="row between"><div><h3>Market Workspace Access</h3><p class="muted">Server-controlled Staff / Customer permissions.</p></div><span class="badge warn">TEST61</span></div><div class="card-list">${filtered.map(r=>{const a=accessOf(r);return `<article class="card" data-kind="${safe(r.kind)}" data-id="${safe(r.id)}"><div class="row between"><div><h4>${safe(r.name||'Unnamed')}</h4><p class="muted">${safe(r.kind)}${r.role_code?` · ${safe(r.role_code)}`:''}${r.mobile?` · ${safe(r.mobile)}`:''}</p></div><span class="badge ${a.menu_enabled?'good':'bad'}">${a.menu_enabled?'MARKET ON':'MARKET OFF'}</span></div><div class="form-checks">${KEYS.map(([k,label])=>`<label class="check"><input type="checkbox" data-access="${k}" ${a[k]?'checked':''}> ${safe(label)}</label>`).join('')}</div><div class="button-row"><button class="primary" data-save-market>Save Market Access</button></div></article>`}).join('')||'<p class="muted">No matching staff/customer.</p>'}</div></div>`;$('marketAccessSearch')?.addEventListener('input',render);$('marketAccessRefresh')?.addEventListener('click',load);root.querySelectorAll('[data-save-market]').forEach(b=>b.addEventListener('click',save));}
async function load(){const root=$('tab-market-access');if(!root)return;const c=client();if(!c){root.innerHTML='<div class="panel"><p class="message error">REDZED session/client not loaded. Open this page from the TEST61 app login.</p></div>';return}root.classList.remove('hidden');root.innerHTML='<div class="panel"><p class="muted">Loading Market Access…</p></div>';try{const {data,error}=await c.rpc('rr_owner_market_access_list_v61');if(error)throw error;rows=[...(data?.staff||[]).map(x=>({...x,kind:'STAFF'})),...(data?.customers||[]).map(x=>({...x,kind:'CUSTOMER'}))];render()}catch(e){root.innerHTML=`<div class="panel"><p class="message error">Market Access load failed: ${safe(e?.message||e)}</p><button id="marketAccessRetry" class="secondary">Retry</button></div>`;$('marketAccessRetry')?.addEventListener('click',load)}}
async function save(ev){const card=ev.currentTarget.closest('[data-kind][data-id]');if(!card)return;const access={};KEYS.forEach(([k])=>access[k]=!!card.querySelector(`[data-access="${k}"]`)?.checked);ev.currentTarget.disabled=true;ev.currentTarget.textContent='Saving…';try{const c=client();if(!c)throw new Error('REDZED session/client not loaded');const {error}=await c.rpc('rr_owner_market_access_set_v61',{p_subject_kind:card.dataset.kind,p_subject_id:card.dataset.id,p_access:access});if(error)throw error;await load()}catch(e){alert(e?.message||e)}finally{if(ev.currentTarget){ev.currentTarget.disabled=false;ev.currentTarget.textContent='Save Market Access'}}}
function install(){const tabs=$('tabs');if(!tabs)return;let sec=$('tab-market-access');let btn=tabs.querySelector('[data-tab="market-access"]');if(!btn){btn=document.createElement('button');btn.dataset.tab='market-access';btn.textContent='Market Access';tabs.appendChild(btn)}if(!sec){sec=document.createElement('section');sec.id='tab-market-access';sec.className='hidden';($('tab-departments')||tabs).after(sec)}const open=()=>{document.querySelectorAll('#tabs button').forEach(x=>x.classList.toggle('active',x===btn));document.querySelectorAll('main.page>section[id^="tab-"]').forEach(x=>x.classList.add('hidden'));sec.classList.remove('hidden');load()};btn.addEventListener('click',open);if(btn.classList.contains('active')||tabs.children.length===1||location.hash==='#market-access')open()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
})();