/* REDZED ERP — Alter / Remake / Damage UI Add-on V3
   Required globals:
     window.supabaseClient  (Supabase JS client)
     window.REDZED_USER = { id,name,role,department_id,department_name,line_man_id,line_man_name,cutting_master_id,cutting_master_name }
   Mount:
     RedzedAlter.mount('#alterRoot', { lotNo: 'LOT-001' });
*/
(function(){
  'use strict';
  const BUCKET='redzed-alter-evidence';
  const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const client=()=>window.supabaseClient||window.supabase;
  const me=()=>window.REDZED_USER||{};
  const role=()=>String(me().role||'WORKER').toUpperCase();
  const isHead=()=>['DEPARTMENT_HEAD','DEPT_HEAD','HEAD','OWNER','ADMIN'].includes(role());
  const isMaster=()=>['CUTTING_MASTER','MASTER','OWNER','ADMIN'].includes(role());
  const isLineMan=()=>['LINE_MAN','LINE_MANAGER','DEPARTMENT_HEAD','OWNER','ADMIN'].includes(role());
  const id=()=>crypto.randomUUID();
  let root,opt={},cards=[],viewerItems=[],viewerIndex=0,cutMap=[];

  function toast(msg,kind=''){ const el=root?.querySelector('[data-alt-msg]'); if(el){el.className='rr-alt-'+kind;el.textContent=msg;} }
  function ensureClient(){ if(!client()?.from) throw new Error('Supabase client unavailable. Set window.supabaseClient.'); }
  function modalHtml(){return `<div class="rr-alt-modal" data-alt-viewer><button class="rr-alt-modal-close" data-alt-close>×</button><button class="rr-alt-modal-prev" data-alt-prev>‹</button><img data-alt-full alt="Evidence"><button class="rr-alt-modal-next" data-alt-next>›</button><div class="rr-alt-modal-caption" data-alt-caption></div></div>`}
  function shell(){root.classList.add('rr-alt-root');root.innerHTML=`<div class="rr-alt-toolbar"><button class="rr-alt-btn" data-new-alter>+ Register Alter</button><button class="rr-alt-btn" data-refresh>Refresh</button><span data-alt-msg></span></div><div data-alt-list></div>${modalHtml()}<div class="rr-alt-sheet" data-alt-sheet><div class="rr-alt-sheet-body" data-alt-sheet-body></div></div>`;bindShell();}
  function bindShell(){root.querySelector('[data-refresh]').onclick=load;root.querySelector('[data-new-alter]').onclick=openAlterForm;root.querySelector('[data-alt-close]').onclick=closeViewer;root.querySelector('[data-alt-prev]').onclick=()=>showViewer(viewerIndex-1);root.querySelector('[data-alt-next]').onclick=()=>showViewer(viewerIndex+1);root.querySelector('[data-alt-viewer]').onclick=e=>{if(e.target.dataset.altViewer!==undefined)closeViewer()};document.addEventListener('keydown',e=>{if(e.key==='Escape')closeViewer();if(e.key==='ArrowLeft')showViewer(viewerIndex-1);if(e.key==='ArrowRight')showViewer(viewerIndex+1)});}
  async function signed(path){const r=await client().storage.from(BUCKET).createSignedUrl(path,3600);return r.error?'':r.data.signedUrl;}
  async function load(){try{ensureClient();toast('Loading…');const r=await client().from('rr_up_alter_card_v1').select('*').eq('lot_no',opt.lotNo).order('created_at',{ascending:false});if(r.error)throw r.error;cards=r.data||[];await Promise.all(cards.map(async a=>{const [l,m,r]=await Promise.all([client().from('rr_up_alter_line_flow_v3').select('*').eq('alter_id',a.id).order('colour_name'),client().from('rr_up_alter_media').select('*').eq('alter_id',a.id).is('deleted_at',null).order('created_at'),client().from('rr_up_alter_repair_v3').select('*').eq('alter_id',a.id).order('created_at',{ascending:false})]);a.lines=l.data||[];a.media=m.data||[];a.repairs=r.data||[];a.media=await Promise.all(a.media.map(async x=>({...x,url:await signed(x.storage_path)})));}));render();toast('');}catch(e){console.error(e);toast(e.message||String(e),'error')}}
  function render(){const list=root.querySelector('[data-alt-list]');list.innerHTML=cards.length?cards.map(cardHtml).join(''):'<div class="rr-alt-card">No Alter registered for this Lot.</div>';bindCards();}
  function cardHtml(a){const unresolved=(a.lines||[]).reduce((n,x)=>n+Number(x.unresolved_alter_qty||0),0);return `<article class="rr-alt-card"><div style="display:flex;justify-content:space-between;gap:8px"><div><div class="rr-alt-title">ALTER REGISTERED : ${a.alter_qty} PCS</div><div class="rr-alt-meta">${esc(a.alter_code)} · ${esc(a.department_name)}</div></div><span class="rr-alt-status">${unresolved>0?'OPEN':'RESOLVED'}</span></div><div class="rr-alt-meta">Registered Worker: <b>${esc(a.worker_name)}</b><br>Responsible Line Man: <b>${esc(a.line_man_name||'—')}</b><br>Cutting Master: <b>${esc(a.cutting_master_name||'—')}</b></div><table class="rr-alt-lines"><thead><tr><th>Colour</th><th>Size</th><th>Alter</th><th>Repair Accepted</th><th>Remake Issued</th><th>Remake Completed</th><th>Damage</th><th>Unresolved</th></tr></thead><tbody>${(a.lines||[]).map(x=>`<tr><td>${esc(x.colour_name)}</td><td>${esc(x.size_code)}</td><td>${x.alter_qty}</td><td>${x.repair_accepted_qty}</td><td>${x.remake_issued_qty}</td><td>${x.remake_completed_qty}</td><td>${x.damage_qty}</td><td>${x.unresolved_alter_qty}</td></tr>`).join('')}</tbody></table><div class="rr-alt-meta"><b>Repair responsibility</b>${(a.repairs||[]).length?(a.repairs||[]).map(r=>`<div>${esc(r.repair_worker_name)} · ${r.assigned_qty} PCS · ${esc(r.verification_status)} · Entered by ${esc(r.assigned_by_name||r.responsible_line_man_name||'')} ${isLineMan()&&['ASSIGNED','RETURNED_FOR_REPAIR'].includes(r.verification_status)?`<button class="rr-alt-btn" data-submit-repair="${r.id}">Submit Repair</button>`:''}${isHead()&&r.verification_status==='REPAIR_SUBMITTED'?`<button class="rr-alt-btn" data-verify-repair="${r.id}">Verify</button>`:''}</div>`).join(''):'<div>No repair assignment.</div>'}</div><div class="rr-alt-thumbs">${(a.media||[]).filter(x=>x.url).map((x,i)=>`<img class="rr-alt-thumb" src="${esc(x.url)}" data-view-alter="${a.id}" data-view-index="${i}" alt="${esc(x.entity_type)} evidence">`).join('')}</div><div class="rr-alt-toolbar">${isLineMan()?`<button class="rr-alt-btn" data-assign-repair="${a.id}">Assign Alter Repair</button>`:''}${isMaster()?`<button class="rr-alt-btn" data-remake="${a.id}">Issue Remake</button><button class="rr-alt-btn" data-complete-remake="${a.id}">Complete Remake</button>`:''}${isHead()?`<button class="rr-alt-btn" data-damage="${a.id}">Damage</button>`:''}</div></article>`}
  function bindCards(){root.querySelectorAll('[data-view-alter]').forEach(el=>el.onclick=()=>{const a=cards.find(x=>x.id===el.dataset.viewAlter);viewerItems=(a?.media||[]).filter(x=>x.url);showViewer(Number(el.dataset.viewIndex)||0)});root.querySelectorAll('[data-remake]').forEach(b=>b.onclick=()=>openLineAction('REMAKE',cards.find(x=>x.id===b.dataset.remake)));root.querySelectorAll('[data-damage]').forEach(b=>b.onclick=()=>openLineAction('DAMAGE',cards.find(x=>x.id===b.dataset.damage)));root.querySelectorAll('[data-assign-repair]').forEach(b=>b.onclick=()=>assignRepair(cards.find(x=>x.id===b.dataset.assignRepair)));root.querySelectorAll('[data-complete-remake]').forEach(b=>b.onclick=()=>completeRemake(cards.find(x=>x.id===b.dataset.completeRemake)));root.querySelectorAll('[data-submit-repair]').forEach(b=>b.onclick=()=>submitRepair(b.dataset.submitRepair));root.querySelectorAll('[data-verify-repair]').forEach(b=>b.onclick=()=>verifyRepair(b.dataset.verifyRepair));}
  async function assignRepair(a){const line=(a.lines||[]).find(x=>Number(x.unresolved_alter_qty)>0);if(!line)return toast('No unresolved Alter available.','error');const worker=prompt('Repair Worker name:');if(!worker?.trim())return;const qty=Number(prompt(`Assign qty (max ${line.unresolved_alter_qty}):`,line.unresolved_alter_qty));if(!qty)return;const r=await client().rpc('rr_up_assign_alter_repair_v3',{p_alter_line_id:line.alter_line_id,p_qty:qty,p_repair_worker_id:null,p_repair_worker_name:worker.trim(),p_note:null});if(r.error)return toast(r.error.message,'error');await load();toast('Alter assigned for repair.','success')}
  async function submitRepair(id){const qty=Number(prompt('Repaired qty submitted by worker:'));if(!qty)return;const r=await client().rpc('rr_up_submit_alter_repair_v3',{p_repair_id:id,p_submitted_qty:qty,p_note:null});if(r.error)return toast(r.error.message,'error');await load();toast('Alter Repair Submitted by Line Man.','success')}
  async function verifyRepair(id){const action=prompt('Type ACCEPT, RETURN or DAMAGE:','ACCEPT');if(!action)return;const qty=Number(prompt('Verified qty:'));if(!qty)return;const r=await client().rpc('rr_up_verify_alter_repair_v3',{p_repair_id:id,p_action:action.toUpperCase(),p_verified_qty:qty,p_note:null});if(r.error)return toast(r.error.message,'error');await load();toast('Repair verification saved.','success')}
  async function completeRemake(a){const line=(a.lines||[]).find(x=>Number(x.remake_issued_qty)>Number(x.remake_completed_qty));if(!line)return toast('No issued Remake pending.','error');const max=Number(line.remake_issued_qty)-Number(line.remake_completed_qty);const qty=Number(prompt(`Completed Remake qty (max ${max}):`,max));if(!qty)return;const r=await client().rpc('rr_up_complete_remake_v3',{p_alter_line_id:line.alter_line_id,p_qty:qty,p_evidence_path:null,p_note:null});if(r.error)return toast(r.error.message,'error');await load();toast('Remake completed.','success')}
  function showViewer(i){if(!viewerItems.length)return;viewerIndex=(i+viewerItems.length)%viewerItems.length;const x=viewerItems[viewerIndex],m=root.querySelector('[data-alt-viewer]');m.querySelector('[data-alt-full]').src=x.url;m.querySelector('[data-alt-caption]').textContent=`${x.entity_type} · ${x.uploaded_by_name||''} · ${new Date(x.created_at).toLocaleString()}`;m.classList.add('open')}
  function closeViewer(){root?.querySelector('[data-alt-viewer]')?.classList.remove('open')}
  function openSheet(html){const s=root.querySelector('[data-alt-sheet]');s.querySelector('[data-alt-sheet-body]').innerHTML=html;s.classList.add('open');s.onclick=e=>{if(e.target===s)closeSheet()}}
  function closeSheet(){root.querySelector('[data-alt-sheet]').classList.remove('open')}
  async function loadCutMap(){
    const r=await client().rpc('rr_up_get_lot_cut_sizes_v5',{p_lot_no:opt.lotNo});
    if(r.error)throw r.error;
    cutMap=Array.isArray(r.data)?r.data:[];
    if(!cutMap.length)throw new Error('इस Lot की Colour-Size cutting mapping नहीं मिली। पहले Cut Colour-Size Mapping save करें।');
  }
  function colourItems(){
    const seen=new Set();
    return cutMap.filter(x=>x.available_alter_qty>0).map(x=>({code:x.colour_code,name:x.colour_name})).filter(x=>{
      const k=String(x.code||x.name).toUpperCase();if(!k||seen.has(k))return false;seen.add(k);return true;
    });
  }
  function colourOptions(selected=''){
    const items=colourItems();
    return '<option value="">Select cut colour</option>'+items.map(c=>`<option value="${esc(c.code)}" data-name="${esc(c.name)}" ${String(selected).toUpperCase()===String(c.code).toUpperCase()?'selected':''}>${esc(c.name)}</option>`).join('');
  }
  function sizeOptions(colourCode,selected=''){
    const rows=cutMap.filter(x=>String(x.colour_code).toUpperCase()===String(colourCode).toUpperCase()&&Number(x.available_alter_qty)>0);
    return '<option value="">Select cut size</option>'+rows.map(x=>`<option value="${esc(x.size_code)}" data-cut="${Number(x.cut_qty)}" data-used="${Number(x.registered_alter_qty)}" data-available="${Number(x.available_alter_qty)}" ${String(selected).toUpperCase()===String(x.size_code).toUpperCase()?'selected':''}>${esc(x.size_code)} · Max ${Number(x.available_alter_qty)} PCS</option>`).join('');
  }
  function updateRowMapping(row){
    const colour=row.querySelector('[data-colour]'),size=row.querySelector('[data-size]'),qty=row.querySelector('[data-qty]'),info=row.querySelector('[data-map-info]');
    size.innerHTML=sizeOptions(colour.value);
    qty.value='';qty.removeAttribute('max');
    info.textContent=colour.value?'अब cut Size select करें।':'Lot में cut हुआ Colour select करें।';
  }
  function updateRowLimit(row){
    const colour=row.querySelector('[data-colour]'),size=row.querySelector('[data-size]'),qty=row.querySelector('[data-qty]'),info=row.querySelector('[data-map-info]');
    const x=cutMap.find(v=>String(v.colour_code).toUpperCase()===String(colour.value).toUpperCase()&&String(v.size_code).toUpperCase()===String(size.value).toUpperCase());
    if(!x){qty.value='';qty.removeAttribute('max');info.textContent='Valid cut Size select करें।';return;}
    qty.max=String(x.available_alter_qty);qty.placeholder=`Max ${x.available_alter_qty}`;
    info.textContent=`Cut ${x.cut_qty} · पहले Alter ${x.registered_alter_qty} · Maximum New Alter ${x.available_alter_qty} PCS`;
    if(Number(qty.value)>Number(x.available_alter_qty))qty.value=x.available_alter_qty;
  }
  function addAlterRow(){
    const box=root.querySelector('[data-alt-rows]');
    box.insertAdjacentHTML('beforeend',`<div class="rr-alt-row"><label class="rr-alt-field">Colour<select data-colour required>${colourOptions()}</select></label><label class="rr-alt-field">Cut Size<select data-size required><option value="">Select colour first</option></select></label><label class="rr-alt-field">Alter Qty<input data-qty type="number" min="1" step="1" inputmode="numeric" placeholder="Select size first" required><small data-map-info>Lot में cut हुआ Colour select करें।</small></label><button type="button" class="rr-alt-btn" data-remove title="Remove colour-size row">×</button></div>`);
    const row=box.lastElementChild;
    row.querySelector('[data-colour]').onchange=()=>updateRowMapping(row);
    row.querySelector('[data-size]').onchange=()=>updateRowLimit(row);
    row.querySelector('[data-qty]').oninput=()=>{
      const q=row.querySelector('[data-qty]');if(q.max&&Number(q.value)>Number(q.max)){q.value=q.max;toast(`Maximum allowed ${q.max} PCS है।`,'error');}
    };
    row.querySelector('[data-remove]').onclick=e=>{if(box.children.length===1)return toast('At least one colour-size row is required.','error');e.currentTarget.parentElement.remove()};
  }
  async function openAlterForm(){
    try{
      toast('Loading cut Colour-Size mapping…');await loadCutMap();toast('');
      openSheet(`<h2>Register Alter</h2><div class="rr-alt-lot-highlight"><small>LOT NO</small><strong>${esc(opt.lotNo)}</strong></div><div class="rr-alt-meta">Colour और Size केवल इसी Lot की cutting mapping से select होंगे। Qty mapped Cut Qty से अधिक नहीं होगी।</div><form data-alter-form><div data-alt-rows></div><button type="button" class="rr-alt-btn" data-add-row>+ Add Next Colour / Size</button><label class="rr-alt-field"><span>Fault Image — Live Camera (Mandatory)</span><input data-camera type="file" accept="image/*" capture="environment" required></label><img class="rr-alt-camera" data-preview hidden><label class="rr-alt-field">Fault Note<textarea data-note placeholder="Optional fault note"></textarea></label><div class="rr-alt-toolbar"><button class="rr-alt-btn" type="submit">Submit & Lock</button><button class="rr-alt-btn" type="button" data-cancel>Cancel</button></div></form>`);
      addAlterRow();root.querySelector('[data-add-row]').onclick=addAlterRow;root.querySelector('[data-cancel]').onclick=closeSheet;
      const input=root.querySelector('[data-camera]'),preview=root.querySelector('[data-preview]');input.onchange=()=>{const f=input.files[0];if(f){preview.src=URL.createObjectURL(f);preview.hidden=false}};
      root.querySelector('[data-alter-form]').onsubmit=submitAlter;
    }catch(e){toast(e.message||String(e),'error')}
  }
  async function uploadEvidence(file,alterId,type,entityId){const user=me(),ext=(file.type.split('/')[1]||'jpg').replace('jpeg','jpg'),path=`${user.id}/${alterId}/${type.toLowerCase()}-${Date.now()}-${id()}.${ext}`;const up=await client().storage.from(BUCKET).upload(path,file,{contentType:file.type,upsert:false});if(up.error)throw up.error;const db=await client().rpc('rr_up_add_media_v1',{p_alter_id:alterId,p_entity_type:type,p_entity_id:entityId||null,p_storage_path:path,p_mime_type:file.type,p_captured_live:true,p_uploaded_by_name:user.name||null});if(db.error)throw db.error;return db.data;}
  async function submitAlter(e){e.preventDefault();try{ensureClient();const user=me(),rows=[...root.querySelectorAll('[data-alt-rows] .rr-alt-row')].map(r=>{const c=r.querySelector('[data-colour]'),o=c.options[c.selectedIndex];return {colour_code:c.value.trim(),colour_name:(o?.dataset.name||o?.textContent||c.value).trim(),size_code:r.querySelector('[data-size]').value.trim(),qty:Number(r.querySelector('[data-qty]').value)}}).filter(x=>x.qty>0);if(!rows.length)throw new Error('At least one mapped Colour-Size row is required.');const totals={};for(const x of rows){const k=(x.colour_code+'|'+x.size_code).toUpperCase();totals[k]=(totals[k]||0)+x.qty;const m=cutMap.find(v=>(v.colour_code+'|'+v.size_code).toUpperCase()===k);if(!m)throw new Error(`${x.colour_name} / ${x.size_code} इस Lot की cutting mapping में नहीं है।`);if(totals[k]>Number(m.available_alter_qty))throw new Error(`${x.colour_name} / ${x.size_code}: Maximum New Alter ${m.available_alter_qty} PCS है।`);}const file=root.querySelector('[data-camera]').files[0];if(!file)throw new Error('Live camera image is required.');const rpc=await client().rpc('rr_up_register_alter_v1',{p_lot_no:opt.lotNo,p_department_id:user.department_id||null,p_department_name:user.department_name||opt.departmentName||'Production',p_worker_id:user.id,p_worker_name:user.name||'Worker',p_line_man_id:user.line_man_id||null,p_line_man_name:user.line_man_name||null,p_cutting_master_id:user.cutting_master_id||null,p_cutting_master_name:user.cutting_master_name||null,p_lines:rows,p_note:root.querySelector('[data-note]').value||null});if(rpc.error)throw rpc.error;await uploadEvidence(file,rpc.data.id,'ALTER',null);closeSheet();await load();toast('Alter registered and locked.','success')}catch(err){toast(err.message||String(err),'error')}}
  function openLineAction(kind,a){const rows=(a.lines||[]).map(x=>`<div class="rr-alt-row" data-action-line="${x.alter_line_id}"><div class="rr-alt-field"><b>${esc(x.colour_name)} / ${esc(x.size_code)}</b><small>Pending ${x.pending_qty}</small></div><label class="rr-alt-field">Qty<input data-action-qty type="number" min="0" max="${x.pending_qty}" value="0"></label></div>`).join('');openSheet(`<h2>${kind==='REMAKE'?'Issue Remake':'Register Damage'} — ${esc(a.alter_code)}</h2><form data-action-form>${rows}${kind==='DAMAGE'?`<label class="rr-alt-field">Fault Worker<input data-fault-worker value="${esc(a.worker_name)}"></label><label class="rr-alt-field">Reason<input data-reason required></label>`:''}<label class="rr-alt-field">Note<textarea data-action-note></textarea></label><div class="rr-alt-toolbar"><button class="rr-alt-btn" type="submit">Save</button><button class="rr-alt-btn" type="button" data-cancel>Cancel</button></div></form>`);root.querySelector('[data-cancel]').onclick=closeSheet;root.querySelector('[data-action-form]').onsubmit=e=>submitLineAction(e,kind,a)}
  async function submitLineAction(e,kind,a){e.preventDefault();try{const lines=[...root.querySelectorAll('[data-action-line]')].map(r=>({alter_line_id:r.dataset.actionLine,qty:Number(r.querySelector('[data-action-qty]').value)})).filter(x=>x.qty>0);if(!lines.length)throw new Error('Enter at least one quantity.');let rpc,entity;if(kind==='REMAKE'){rpc=await client().rpc('rr_up_issue_remake_v1',{p_alter_id:a.id,p_lines:lines,p_issued_by_name:me().name||null,p_note:root.querySelector('[data-action-note]').value||null});entity=rpc.data?.id}else{rpc=await client().rpc('rr_up_save_damage_v1',{p_alter_id:a.id,p_damage_id:null,p_fault_worker_id:a.worker_id,p_fault_worker_name:root.querySelector('[data-fault-worker]').value||a.worker_name,p_reason:root.querySelector('[data-reason]').value,p_note:root.querySelector('[data-action-note]').value||null,p_lines:lines});entity=rpc.data?.id}if(rpc.error)throw rpc.error;closeSheet();await load();toast(`${kind} saved colour-size wise.`,'success')}catch(err){toast(err.message||String(err),'error')}}
  function openStatus(a){openSheet(`<h2>Update Status — ${esc(a.alter_code)}</h2><form data-status-form><label class="rr-alt-field">Status<select data-new-status>${['OPEN','PARTIAL','REMAKE_ISSUED','READY_TO_VERIFY','CLOSED','CANCELLED'].map(x=>`<option ${x===a.status?'selected':''}>${x}</option>`).join('')}</select></label><label class="rr-alt-field">Note<textarea data-status-note></textarea></label><div class="rr-alt-toolbar"><button class="rr-alt-btn">Save</button><button type="button" class="rr-alt-btn" data-cancel>Cancel</button></div></form>`);root.querySelector('[data-cancel]').onclick=closeSheet;root.querySelector('[data-status-form]').onsubmit=async e=>{e.preventDefault();const r=await client().rpc('rr_up_set_alter_status_v1',{p_alter_id:a.id,p_status:root.querySelector('[data-new-status]').value,p_note:root.querySelector('[data-status-note]').value||null});if(r.error)return toast(r.error.message,'error');closeSheet();await load();toast('Status updated.','success')}}
  window.RedzedAlter={mount(selector,options){root=typeof selector==='string'?document.querySelector(selector):selector;if(!root)throw new Error('Alter mount element not found.');opt={...options};if(!opt.lotNo)throw new Error('lotNo is required.');shell();load();},reload:load};
})();
