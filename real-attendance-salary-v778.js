(()=>{
'use strict';
window.REAL FACTORY_ATTENDANCE_SALARY_VERSION='778.2.0';
const state={client:null,auth:null,workers:[],attendanceWorkerId:'',adjustmentWorkerId:'',attendance:[],holidays:[],run:null,lines:[],adjustments:[]};
const $=id=>document.getElementById(id);
const safe=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const err=e=>[e?.message,e?.details,e?.hint,e?.code].filter(Boolean).join(' — ')||'Unknown error';
const money=v=>Number(v||0).toLocaleString('en-IN',{minimumFractionDigits:2,maximumFractionDigits:2});
const upper=v=>String(v||'').trim().toUpperCase();
const monthStart=value=>`${value}-01`;
const monthRange=value=>{const [y,m]=value.split('-').map(Number);const start=new Date(Date.UTC(y,m-1,1));const end=new Date(Date.UTC(y,m,0));return [start.toISOString().slice(0,10),end.toISOString().slice(0,10)]};
const localISO=(date,time)=>date&&time?new Date(`${date}T${time}:00+05:30`).toISOString():null;
const localTime=value=>value?new Date(value).toLocaleTimeString('en-IN',{timeZone:'Asia/Kolkata',hour:'2-digit',minute:'2-digit',hour12:false}):'—';
const dayName=d=>new Date(`${d}T00:00:00+05:30`).toLocaleDateString('en-IN',{weekday:'short',timeZone:'Asia/Kolkata'});
function say(id,text,type=''){const el=$(id);el.textContent=text||'';el.className=`message ${type}`.trim()}
async function rpc(name,payload={}){const r=await state.client.rpc(name,payload);if(r.error)throw r.error;return r.data}
function todayMonth(){return new Date().toLocaleDateString('en-CA',{timeZone:'Asia/Kolkata',year:'numeric',month:'2-digit'}).slice(0,7)}
function todayDate(){return new Date().toLocaleDateString('en-CA',{timeZone:'Asia/Kolkata'})}

class SearchSelect{
  constructor(select){this.select=select;this.wrap=document.createElement('div');this.wrap.className='combo';this.input=document.createElement('input');this.input.className='combo-input';this.input.autocomplete='off';this.list=document.createElement('div');this.list.className='combo-list hidden';select.parentNode.insertBefore(this.wrap,select);this.wrap.append(this.input,this.list);this.wrap.append(select);select.classList.add('hidden');select._searchCombo=this;this.input.addEventListener('focus',()=>this.render());this.input.addEventListener('input',()=>this.render());this.input.addEventListener('keydown',e=>this.key(e));document.addEventListener('click',e=>{if(!this.wrap.contains(e.target))this.list.classList.add('hidden')});this.sync()}
  options(){return [...this.select.options].map((o,i)=>({i,value:o.value,label:o.textContent.trim(),disabled:o.disabled}))}
  sync(){const o=this.select.selectedOptions[0];this.input.value=o?.textContent.trim()||''}
  render(){const q=this.input.value.trim().toLowerCase();this.filtered=this.options().filter(o=>!o.disabled&&(!q||o.label.toLowerCase().includes(q)));this.index=-1;this.list.innerHTML=this.filtered.length?this.filtered.map((o,i)=>`<div class="combo-option" data-si="${i}">${safe(o.label)}</div>`).join(''):'<div class="combo-option muted">No option</div>';this.list.classList.remove('hidden');this.list.querySelectorAll('[data-si]').forEach(el=>el.onclick=()=>this.pick(Number(el.dataset.si)))}
  pick(i){const o=this.filtered[i];if(!o)return;this.select.value=o.value;this.sync();this.list.classList.add('hidden');this.select.dispatchEvent(new Event('change',{bubbles:true}))}
  key(e){if(this.list.classList.contains('hidden'))return;if(e.key==='ArrowDown'){e.preventDefault();this.index=Math.min(this.filtered.length-1,this.index+1)}else if(e.key==='ArrowUp'){e.preventDefault();this.index=Math.max(0,this.index-1)}else if(e.key==='Enter'&&this.index>=0){e.preventDefault();this.pick(this.index);return}else if(e.key==='Escape'){this.list.classList.add('hidden');return}else return;[...this.list.querySelectorAll('[data-si]')].forEach((x,i)=>x.classList.toggle('active',i===this.index))}
}
function upgradeSelects(){document.querySelectorAll('select').forEach(s=>{if(!s._searchCombo)new SearchSelect(s)})}
function syncSelects(){document.querySelectorAll('select').forEach(s=>s._searchCombo?.sync())}

class SearchCombo{
  constructor(root,onSelect){this.root=root;this.onSelect=onSelect;this.rows=[];this.filtered=[];this.index=-1;this.value='';this.root.innerHTML='<div class="combo"><input class="combo-input" autocomplete="off" placeholder="Type worker name / code / department"><div class="combo-list hidden"></div></div>';this.input=this.root.querySelector('.combo-input');this.list=this.root.querySelector('.combo-list');this.input.addEventListener('input',()=>this.render());this.input.addEventListener('focus',()=>this.render());this.input.addEventListener('keydown',e=>this.key(e));document.addEventListener('click',e=>{if(!this.root.contains(e.target))this.list.classList.add('hidden')})}
  setRows(rows){this.rows=rows||[];this.render()}
  setValue(id){const row=this.rows.find(x=>String(x.worker_id)===String(id));this.value=row?.worker_id||'';this.input.value=row?`${row.worker_name} · ${row.worker_code||'—'} · ${row.department_code||'—'}`:''}
  render(){const q=this.input.value.trim().toLowerCase();this.filtered=this.rows.filter(r=>!q||JSON.stringify([r.worker_name,r.worker_code,r.department_code]).toLowerCase().includes(q)).slice(0,80);this.index=-1;this.list.innerHTML=this.filtered.length?this.filtered.map((r,i)=>`<div class="combo-option" data-i="${i}"><b>${safe(r.worker_name||'Unnamed')}</b><small>${safe(r.worker_code||'—')} · ${safe(r.department_code||'—')} · ₹${money(r.monthly_salary)}</small></div>`).join(''):'<div class="combo-option muted">No matching salaried worker</div>';this.list.classList.remove('hidden');this.list.querySelectorAll('[data-i]').forEach(el=>el.onclick=()=>this.pick(Number(el.dataset.i)))}
  pick(i){const row=this.filtered[i];if(!row)return;this.value=row.worker_id;this.input.value=`${row.worker_name} · ${row.worker_code||'—'} · ${row.department_code||'—'}`;this.list.classList.add('hidden');this.onSelect?.(row)}
  key(e){if(this.list.classList.contains('hidden'))return;if(e.key==='ArrowDown'){e.preventDefault();this.index=Math.min(this.filtered.length-1,this.index+1)}else if(e.key==='ArrowUp'){e.preventDefault();this.index=Math.max(0,this.index-1)}else if(e.key==='Enter'&&this.index>=0){e.preventDefault();this.pick(this.index);return}else if(e.key==='Escape'){this.list.classList.add('hidden');return}else return;[...this.list.querySelectorAll('[data-i]')].forEach((x,i)=>x.classList.toggle('active',i===this.index))}
}
let attendanceCombo,adjustmentCombo;

async function loadWorkers(mode){const r=await state.client.from('rr_worker_payroll_board_v777_3').select('*').eq('data_mode',mode).eq('worker_category','SALARIED').order('worker_name',{ascending:true});if(r.error)throw r.error;const byId=new Map();for(const row of r.data||[]){if(!byId.has(String(row.worker_id)))byId.set(String(row.worker_id),row)}state.workers=[...byId.values()];attendanceCombo.setRows(state.workers);adjustmentCombo.setRows(state.workers);if(state.attendanceWorkerId&&state.workers.some(w=>String(w.worker_id)===String(state.attendanceWorkerId)))attendanceCombo.setValue(state.attendanceWorkerId);if(state.adjustmentWorkerId&&state.workers.some(w=>String(w.worker_id)===String(state.adjustmentWorkerId)))adjustmentCombo.setValue(state.adjustmentWorkerId)}

function calendarDates(from,to){const out=[];for(let d=new Date(`${from}T00:00:00Z`),e=new Date(`${to}T00:00:00Z`);d<=e;d.setUTCDate(d.getUTCDate()+1))out.push(d.toISOString().slice(0,10));return out}
function attendanceCalendarRows(){if(!state.attendanceWorkerId||!$('attendanceMonth').value)return state.attendance;const [monthFrom,monthTo]=monthRange($('attendanceMonth').value);const worker=state.workers.find(w=>String(w.worker_id)===String(state.attendanceWorkerId));const from=worker?.effective_from&&worker.effective_from>monthFrom?worker.effective_from:monthFrom;const to=worker?.effective_to&&worker.effective_to<monthTo?worker.effective_to:monthTo;const byDate=new Map(state.attendance.map(x=>[x.attendance_date,x]));const holidays=new Map(state.holidays.filter(h=>h.is_active!==false).map(h=>[h.holiday_date,h]));return calendarDates(from,to).map(date=>{if(byDate.has(date))return byDate.get(date);const monday=dayName(date)==='Mon';const holiday=holidays.get(date);return {id:null,attendance_date:date,status:holiday?'HOLIDAY':monday?'WEEKLY_OFF':'ABSENT',check_in_at:null,check_out_at:null,raw_late_minutes:0,early_minutes:0,after_shift_minutes:0,is_weekly_off:monday,is_holiday:Boolean(holiday),revision_no:0,locked_payroll_run_id:null,is_virtual:true,holiday_name:holiday?.holiday_name||null}})}
function renderHolidays(){$('holidayList').innerHTML=state.holidays.length?state.holidays.map(h=>`<span class="badge ${h.is_active!==false?'good':'bad'}">${safe(h.holiday_date)} · ${safe(h.holiday_name)} · +${Number(h.extra_pay_factor||0).toFixed(2)} day ${h.is_active!==false?`<button class="btn danger" data-disable-holiday="${safe(h.holiday_date)}" type="button">Disable</button>`:''}</span>`).join(''):'<span class="muted">No other holidays in this month.</span>';$('holidayList').querySelectorAll('[data-disable-holiday]').forEach(b=>b.onclick=()=>disableHoliday(b.dataset.disableHoliday))}
function renderAttendance(){const rows=attendanceCalendarRows();const counts=k=>rows.filter(x=>x.status===k).length;const late=rows.reduce((a,x)=>a+Number(x.raw_late_minutes||0),0);const ot=rows.reduce((a,x)=>a+Number(x.early_minutes||0)+Number(x.after_shift_minutes||0),0);$('attendanceStats').innerHTML=[['Present',counts('PRESENT')],['Absent / Missing',counts('ABSENT')],['Half Day',counts('HALF_DAY')],['Paid Leave',counts('LEAVE_PAID')],['Raw Late Min',late],['Raw OT Min',ot]].map(([a,b])=>`<div class="stat"><small>${a}</small><strong>${b}</strong></div>`).join('');$('attendanceBody').innerHTML=rows.length?rows.map(x=>`<tr><td>${safe(x.attendance_date)}</td><td>${dayName(x.attendance_date)}</td><td class="status-${safe(x.status)}"><b>${safe(x.status)}</b>${x.is_virtual&&x.status==='ABSENT'?'<br><span class="badge bad">MISSING → ABSENT</span>':''}${x.holiday_name?`<br><small>${safe(x.holiday_name)}</small>`:''}</td><td>${localTime(x.check_in_at)}</td><td>${localTime(x.check_out_at)}</td><td>${Number(x.raw_late_minutes||0)}</td><td>${Number(x.early_minutes||0)}</td><td>${Number(x.after_shift_minutes||0)}</td><td>${x.is_weekly_off?'YES':'—'}</td><td>${x.is_holiday?'YES':'—'}</td><td>${x.revision_no}</td><td>${x.locked_payroll_run_id?'<span class="badge bad">LOCKED</span>':'OPEN'}</td><td><button class="btn" ${x.id?`data-edit-att="${safe(x.id)}"`:`data-new-att="${safe(x.attendance_date)}"`}>${x.id?'Edit':'Mark'}</button></td></tr>`).join(''):'<tr><td colspan="13" class="muted">No dates in active payroll period.</td></tr>';$('attendanceBody').querySelectorAll('[data-edit-att]').forEach(b=>b.onclick=()=>editAttendance(b.dataset.editAtt));$('attendanceBody').querySelectorAll('[data-new-att]').forEach(b=>b.onclick=()=>editAttendanceDate(b.dataset.newAtt));renderHolidays()}


async function loadAttendance(){try{say('attendanceMessage','Loading…');const mode=$('attendanceMode').value;await loadWorkers(mode);if(!state.attendanceWorkerId)throw new Error('Search and select a salaried worker.');const [from,to]=monthRange($('attendanceMonth').value);const [r,h]=await Promise.all([state.client.from('rr_attendance_day_board_v778').select('*').eq('worker_id',state.attendanceWorkerId).eq('data_mode',mode).gte('attendance_date',from).lte('attendance_date',to).order('attendance_date',{ascending:true}),state.client.from('rr_payroll_holidays_v778').select('*').gte('holiday_date',from).lte('holiday_date',to).order('holiday_date')]);if(r.error)throw r.error;if(h.error)throw h.error;state.attendance=r.data||[];state.holidays=h.data||[];renderAttendance();say('attendanceMessage',`${state.attendance.length} saved records; missing working dates are shown as ABSENT.`,'success')}catch(e){say('attendanceMessage',err(e),'error')}}
function editAttendanceDate(date){clearAttendanceForm();$('attendanceDate').value=date;syncSelects();window.scrollTo({top:$('attendanceForm').offsetTop-20,behavior:'smooth'})}
function editAttendance(id){const x=state.attendance.find(r=>r.id===id);if(!x)return;$('attendanceDate').value=x.attendance_date;$('attendanceStatus').value=x.status;$('checkInTime').value=x.check_in_at?new Date(x.check_in_at).toLocaleTimeString('en-GB',{timeZone:'Asia/Kolkata',hour:'2-digit',minute:'2-digit'}):'';$('checkOutTime').value=x.check_out_at?new Date(x.check_out_at).toLocaleTimeString('en-GB',{timeZone:'Asia/Kolkata',hour:'2-digit',minute:'2-digit'}):'';$('attendanceHoliday').checked=x.is_holiday;$('attendanceSource').value='CORRECTION';$('attendanceRemarks').value=x.remarks||'';$('attendanceReason').value='Attendance correction verified';$('attendanceFormTitle').textContent=`Correct Attendance · ${x.attendance_date}`;syncSelects();window.scrollTo({top:$('attendanceForm').offsetTop-20,behavior:'smooth'})}
function clearAttendanceForm(){$('attendanceDate').value=todayDate();$('attendanceStatus').value='AUTO';$('checkInTime').value='';$('checkOutTime').value='';$('attendanceHoliday').checked=false;$('attendanceSource').value='MANUAL';$('attendanceRemarks').value='';$('attendanceReason').value='';$('attendanceFormTitle').textContent='Mark / Correct Attendance';syncSelects()}
async function saveAttendance(e){e.preventDefault();try{if(!state.attendanceWorkerId)throw new Error('Select a salaried worker first.');const date=$('attendanceDate').value;const status=$('attendanceStatus').value;const checkIn=localISO(date,$('checkInTime').value);const checkOut=localISO(date,$('checkOutTime').value);say('attendanceMessage','Saving…');await rpc('rr_attendance_save_v778',{p_worker_id:state.attendanceWorkerId,p_attendance_date:date,p_check_in_at:checkIn,p_check_out_at:checkOut,p_status:status,p_is_holiday:$('attendanceHoliday').checked,p_source_type:$('attendanceSource').value,p_remarks:$('attendanceRemarks').value||null,p_reason:$('attendanceReason').value,p_data_mode:$('attendanceMode').value});clearAttendanceForm();await loadAttendance();say('attendanceMessage','Attendance saved with audit history.','success')}catch(e2){say('attendanceMessage',err(e2),'error')}}

async function saveHoliday(e){e.preventDefault();try{const row={holiday_date:$('holidayDate').value,holiday_name:$('holidayName').value.trim(),extra_pay_factor:Number($('holidayFactor').value||0.5),is_active:true,remarks:$('holidayRemarks').value.trim()||null};if(!row.holiday_name)throw new Error('Holiday name required.');const r=await state.client.from('rr_payroll_holidays_v778').upsert(row,{onConflict:'holiday_date'}).select().single();if(r.error)throw r.error;$('holidayName').value='';$('holidayRemarks').value='';await loadAttendance();say('holidayMessage','Holiday saved.','success')}catch(e2){say('holidayMessage',err(e2),'error')}}
async function disableHoliday(date){try{const reason=prompt('Disable holiday reason','Holiday cancelled / wrong date')||'';if(!reason)return;const r=await state.client.from('rr_payroll_holidays_v778').update({is_active:false,remarks:reason}).eq('holiday_date',date);if(r.error)throw r.error;await loadAttendance();say('holidayMessage','Holiday disabled.','success')}catch(e){say('holidayMessage',err(e),'error')}}

function renderPayroll(){const r=state.run;const lines=state.lines;$('payrollStats').innerHTML=[['Status',r?.status||'NOT CALCULATED'],['Workers',r?.worker_count||0],['Incomplete',r?.incomplete_worker_count||0],['Gross ₹',money(r?.gross_total)],['Deduction ₹',money(r?.deduction_total)],['Net ₹',money(r?.net_total)]].map(([a,b])=>`<div class="stat"><small>${a}</small><strong>${safe(b)}</strong></div>`).join('');$('payrollBody').innerHTML=lines.length?lines.map(x=>`<tr><td><b>${safe(x.worker_name||x.worker_id)}</b></td><td>${safe(x.department_code||'—')}</td><td>${x.present_days}</td><td>${x.absent_days}</td><td>${x.half_days}</td><td>${x.paid_leave_days}</td><td>${x.unpaid_leave_days}</td><td>${x.holiday_work_days}</td><td>${x.incomplete_days}</td><td>${x.deductible_late_minutes}</td><td>${x.payable_overtime_minutes}</td><td class="money">${money(x.base_salary)}</td><td class="money">${money(x.overtime_earning)}</td><td class="money">${money(x.holiday_extra_earning)}</td><td class="money">${money(x.adjustment_earning)}</td><td class="money">${money(Number(x.absence_deduction)+Number(x.late_deduction)+Number(x.adjustment_deduction))}</td><td class="money">${money(x.advance_deduction)}</td><td class="money"><b>${money(x.net_pay)}</b></td></tr>`).join(''):'<tr><td colspan="18" class="muted">Calculate payroll to create snapshot lines.</td></tr>';const locked=['APPROVED','PAID'].includes(r?.status);$('approvePayroll').disabled=!r||r.status!=='CALCULATED';$('reopenPayroll').disabled=!locked;$('markPaid').disabled=!r||r.status!=='APPROVED';$('calculatePayroll').disabled=locked}
async function loadPayroll(){const month=monthStart($('payrollMonth').value),mode=$('payrollMode').value;const rr=await state.client.from('rr_payroll_run_board_v778').select('*').eq('period_month',month).eq('data_mode',mode).maybeSingle();if(rr.error)throw rr.error;state.run=rr.data||null;if(state.run){const lr=await state.client.from('rr_payroll_line_board_v778').select('*').eq('payroll_run_id',state.run.id).order('worker_name');if(lr.error)throw lr.error;state.lines=lr.data||[]}else state.lines=[];renderPayroll()}
async function calculatePayroll(){try{say('payrollMessage','Calculating authoritative monthly snapshot…');const id=await rpc('rr_payroll_calculate_v778',{p_period_month:monthStart($('payrollMonth').value),p_data_mode:$('payrollMode').value});await loadPayroll();say('payrollMessage',`Payroll calculated. Run ${id}`,'success')}catch(e){say('payrollMessage',err(e),'error')}}
async function approvePayroll(){try{if(!state.run)throw new Error('Calculate payroll first.');const reason=prompt('Approval reason','Attendance checked and payroll approved')||'';if(!reason)return;await rpc('rr_payroll_approve_v778',{p_payroll_run_id:state.run.id,p_reason:reason});await loadPayroll();say('payrollMessage','Payroll approved; attendance and adjustments locked.','success')}catch(e){say('payrollMessage',err(e),'error')}}
async function reopenPayroll(){try{if(!state.run)throw new Error('Payroll run missing.');const reason=prompt('OWNER reopen reason','Correction required')||'';if(!reason)return;await rpc('rr_payroll_reopen_v778',{p_payroll_run_id:state.run.id,p_reason:reason});await loadPayroll();say('payrollMessage','Payroll reopened. Recalculate after corrections.','success')}catch(e){say('payrollMessage',err(e),'error')}}
async function markPaid(){
  const month=$('payrollMonth').value||todayMonth();
  const mode=$('payrollMode').value||'REAL';
  location.href=
    `real-salary-bulk-payment-v782.html?category=SALARIED&cycle=MONTHLY_PART&month=${encodeURIComponent(month)}&mode=${encodeURIComponent(mode)}`;
}

function renderAdjustments(){$('adjustmentBody').innerHTML=state.adjustments.length?state.adjustments.map(x=>`<tr><td>${safe(x.worker_name||x.worker_id)}</td><td>${safe(x.period_month)}</td><td>${safe(x.adjustment_type)}</td><td class="money">${money(x.amount)}</td><td>${safe(x.reason)}</td><td>${safe(x.status)}</td><td>${x.included_payroll_run_id?'YES':'—'}</td><td>${x.status==='POSTED'?`<button class="btn danger" data-cancel-adj="${x.id}">Cancel</button>`:'—'}</td></tr>`).join(''):'<tr><td colspan="8" class="muted">No adjustments.</td></tr>';$('adjustmentBody').querySelectorAll('[data-cancel-adj]').forEach(b=>b.onclick=()=>cancelAdjustment(b.dataset.cancelAdj))}
async function loadAdjustments(){try{await loadWorkers($('adjustmentMode').value);const r=await state.client.from('rr_payroll_adjustment_board_v778').select('*').eq('period_month',monthStart($('adjustmentMonth').value)).eq('data_mode',$('adjustmentMode').value).order('created_at',{ascending:false});if(r.error)throw r.error;state.adjustments=r.data||[];renderAdjustments();say('adjustmentMessage',`${state.adjustments.length} adjustments loaded.`,'success')}catch(e){say('adjustmentMessage',err(e),'error')}}
async function saveAdjustment(e){e.preventDefault();try{if(!state.adjustmentWorkerId)throw new Error('Search and select a salaried worker.');await rpc('rr_payroll_add_adjustment_v778',{p_worker_id:state.adjustmentWorkerId,p_period_month:monthStart($('adjustmentMonth').value),p_adjustment_type:$('adjustmentType').value,p_amount:Number($('adjustmentAmount').value),p_reason:$('adjustmentReason').value,p_data_mode:$('adjustmentMode').value});$('adjustmentAmount').value='';$('adjustmentReason').value='';await loadAdjustments();say('adjustmentMessage','Adjustment posted. Recalculate payroll to include it.','success')}catch(e2){say('adjustmentMessage',err(e2),'error')}}
async function cancelAdjustment(id){try{const reason=prompt('Cancellation reason','Wrong adjustment entry')||'';if(!reason)return;await rpc('rr_payroll_cancel_adjustment_v778',{p_adjustment_id:id,p_reason:reason});await loadAdjustments();say('adjustmentMessage','Adjustment cancelled.','success')}catch(e){say('adjustmentMessage',err(e),'error')}}

function showTab(tab){['attendance','payroll','adjustments'].forEach(t=>{$(`tab-${t}`).classList.toggle('hidden',t!==tab);document.querySelector(`[data-tab="${t}"]`).classList.toggle('active',t===tab)});if(tab==='payroll')loadPayroll().catch(e=>say('payrollMessage',err(e),'error'));if(tab==='adjustments')loadAdjustments()}
function bind(){attendanceCombo=new SearchCombo($('attendanceWorkerCombo'),row=>{state.attendanceWorkerId=row.worker_id;loadAttendance()});adjustmentCombo=new SearchCombo($('adjustmentWorkerCombo'),row=>{state.adjustmentWorkerId=row.worker_id});$('tabs').querySelectorAll('button').forEach(b=>b.onclick=()=>showTab(b.dataset.tab));$('loadAttendance').onclick=loadAttendance;$('attendanceForm').onsubmit=saveAttendance;$('clearAttendanceForm').onclick=clearAttendanceForm;$('attendanceMode').onchange=()=>{state.attendanceWorkerId='';attendanceCombo.setValue('');loadWorkers($('attendanceMode').value)};$('attendanceMonth').onchange=()=>state.attendanceWorkerId&&loadAttendance();$('calculatePayroll').onclick=calculatePayroll;$('approvePayroll').onclick=approvePayroll;$('reopenPayroll').onclick=reopenPayroll;$('markPaid').onclick=markPaid;$('payrollMonth').onchange=()=>loadPayroll();$('payrollMode').onchange=()=>loadPayroll();$('holidayForm').onsubmit=saveHoliday;$('adjustmentForm').onsubmit=saveAdjustment;$('refreshAdjustments').onclick=loadAdjustments;$('adjustmentMode').onchange=()=>{state.adjustmentWorkerId='';adjustmentCombo.setValue('');loadAdjustments()};$('adjustmentMonth').onchange=loadAdjustments;upgradeSelects()}
async function boot(){try{state.client=window.supabaseClient||window.supabaseDb||window.redzedSupabase||window.sb;if(!state.client)throw new Error('Supabase client unavailable. Check config.js.');if(window.RR?.requireRoles)state.auth=await RR.requireRoles(['owner','admin','account','manager','payroll','hr']);else{const s=await state.client.auth.getSession();if(!s.data?.session)throw new Error('Login required.')}bind();
const requestedMode=String(
  new URLSearchParams(location.search).get('mode')||''
).toUpperCase();
if(window.RRDataModeReadyPromise){
  await window.RRDataModeReadyPromise;
}
const initialMode=window.RRDataMode
  ?await RRDataMode.applyInitialModes(
    ['attendanceMode','payrollMode','adjustmentMode'],
    requestedMode
  )
  :(['REAL','TEST'].includes(requestedMode)?requestedMode:'TEST');
if(!window.RRDataMode){
  ['attendanceMode','payrollMode','adjustmentMode']
    .forEach(id=>{if($(id))$(id).value=initialMode});
}
const m=todayMonth();$('attendanceMonth').value=m;$('payrollMonth').value=m;$('adjustmentMonth').value=m;$('attendanceDate').value=todayDate();await loadWorkers(initialMode);await loadPayroll();$('accessBadge').textContent=`ACCESS OK · ${upper(state.auth?.role_code||state.auth?.profile?.role_code||'AUTH')}`;$('accessBadge').className='badge good';window.RR?.startAccessGuard?.()}catch(e){console.error(e);$('accessBadge').textContent='ACCESS ERROR';$('accessBadge').className='badge bad';say('attendanceMessage',err(e),'error')}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
